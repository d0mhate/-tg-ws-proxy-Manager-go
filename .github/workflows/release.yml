name: release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    env:
      CGO_ENABLED: 0

    steps:
      - name: checkout
        uses: actions/checkout@v6

      - name: setup go
        uses: actions/setup-go@v6
        with:
          go-version: "1.22"
          cache: false

      - name: run tests
        run: go test ./...

      - name: build openwrt binaries
        run: |
          mkdir -p build
          GOOS=linux GOARCH=mipsle GOMIPS=softfloat go build -trimpath -ldflags="-s -w" -o build/tg-ws-proxy-openwrt-mipsel_24kc ./cmd/tg-ws-proxy
          GOOS=linux GOARCH=mips GOMIPS=softfloat go build -trimpath -ldflags="-s -w" -o build/tg-ws-proxy-openwrt-mips_24kc ./cmd/tg-ws-proxy
          GOOS=linux GOARCH=amd64 go build -trimpath -ldflags="-s -w" -o build/tg-ws-proxy-openwrt-x86_64 ./cmd/tg-ws-proxy
          GOOS=linux GOARCH=arm GOARM=7 go build -trimpath -ldflags="-s -w" -o build/tg-ws-proxy-openwrt-armv7 ./cmd/tg-ws-proxy
          GOOS=linux GOARCH=arm64 go build -trimpath -ldflags="-s -w" -o build/tg-ws-proxy-openwrt-aarch64 ./cmd/tg-ws-proxy
          GOOS=linux GOARCH=mipsle GOMIPS=softfloat go build -trimpath -ldflags="-s -w" -o build/tg-ws-mtproto-openwrt-mipsel_24kc ./cmd/tg-ws-mtproto
          GOOS=linux GOARCH=mips GOMIPS=softfloat go build -trimpath -ldflags="-s -w" -o build/tg-ws-mtproto-openwrt-mips_24kc ./cmd/tg-ws-mtproto
          GOOS=linux GOARCH=amd64 go build -trimpath -ldflags="-s -w" -o build/tg-ws-mtproto-openwrt-x86_64 ./cmd/tg-ws-mtproto
          GOOS=linux GOARCH=arm GOARM=7 go build -trimpath -ldflags="-s -w" -o build/tg-ws-mtproto-openwrt-armv7 ./cmd/tg-ws-mtproto
          GOOS=linux GOARCH=arm64 go build -trimpath -ldflags="-s -w" -o build/tg-ws-mtproto-openwrt-aarch64 ./cmd/tg-ws-mtproto
          cp build/tg-ws-proxy-openwrt-mipsel_24kc build/tg-ws-proxy-openwrt
          cp build/tg-ws-mtproto-openwrt-mipsel_24kc build/tg-ws-mtproto-openwrt

      - name: publish release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            build/tg-ws-proxy-openwrt-mipsel_24kc
            build/tg-ws-proxy-openwrt-mips_24kc
            build/tg-ws-proxy-openwrt-x86_64
            build/tg-ws-proxy-openwrt-armv7
            build/tg-ws-proxy-openwrt-aarch64
            build/tg-ws-proxy-openwrt
            build/tg-ws-mtproto-openwrt-mipsel_24kc
            build/tg-ws-mtproto-openwrt-mips_24kc
            build/tg-ws-mtproto-openwrt-x86_64
            build/tg-ws-mtproto-openwrt-armv7
            build/tg-ws-mtproto-openwrt-aarch64
            build/tg-ws-mtproto-openwrt
            tg-ws-proxy-go.sh
